# 贡献规则

阅读 AGENTS.md 和 docs/ADDING_COMPONENT.md。建立独立分支，先补或修改组件契约，再更新实现、样式、类型、演示和测试。

提交前执行 npm run check 与浏览器测试；给出变更前后截图和兼容性影响。只改品牌色时优先 Token，不拷贝组件。业务 API/权限/校验留在方案或应用层。

提交内容必须为自己有权提供的代码和资产；引用代码保留归属。禁止密钥、个人信息、真实账号数据进入仓库。新依赖需要说明来源、许可证、维护成本与替代方案。
