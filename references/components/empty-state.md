# YkEmptyState · 空状态

## 视觉
使用统一品牌、前景、圆角和间距变量；工作台按实际能力暴露可调参数。

## 结构
section → illustration + h3 + description + action

## 交互规则
不把空状态当错误；主按钮只发 action，由业务处理。

## 规范与边界
无远程插图下载；自定义图使用 illustration 插槽。

## 来源
原生 HTML 行为 + 独立 Vue 实现。参考入口不表示抄录第三方组件源码。
