# YkPagination · 分页

## 视觉
使用统一品牌、前景、圆角和间距变量；工作台按实际能力暴露可调参数。

## 结构
nav → previous / numbered buttons / next

## 交互规则
首尾按钮按边界禁用；当前页 aria-current；页数减少时显示值收敛。

## 规范与边界
只发出页码，不请求数据；没有页容量选择器和快速跳页。

## 来源
原生 HTML 行为 + 独立 Vue 实现。参考入口不表示抄录第三方组件源码。
