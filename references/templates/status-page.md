# 状态页模板四维规格

视觉：主题文字、状态数字、说明和操作区，支持直角到圆角、单/双列、背景渐变与小屏尺寸限制。

结构：section + 装饰状态图形 + 标题/说明 + 原生按钮；数字图形是 CSS/文本自有实现，不含外部素材。

交互：primary/secondary 事件，不假装已导航或重试。无循环闪烁；动效可关闭。

规范：404/403/500/maintenance 共用 YkStatusPage；不将四种状态计成四套底层实现。真实服务的状态码和认证必须在业务层处理。
